# .gitignore
# GitHub에 올리면 안 되는 파일/폴더를 지정합니다.

# ── API 키 (절대 GitHub에 올리면 안 됩니다!) ──
.env

# ── 자동 생성 폴더 ──
results/
cache/

# ── Python 캐시 ──
__pycache__/
*.py[cod]
*.pyo

# ── 가상환경 폴더 ──
venv/
.venv/
env/

# ── IDE 설정 ──
.vscode/
.idea/

# ── macOS 시스템 파일 ──
.DS_Store