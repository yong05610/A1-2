# =============================================================================
# requirements.txt  ·  프로젝트 의존 라이브러리
# =============================================================================
# 설치 방법: pip install -r requirements.txt
# =============================================================================

# Google Gemini API 클라이언트
# → 여행지 추천 & 최종 리포트 생성에 사용
google-generativeai>=0.7.2

# HTTP 요청 라이브러리
# → Kakao Local API 호출에 사용
requests>=2.32.3

# .env 파일 로드 라이브러리
# → API 키를 코드에 직접 쓰지 않고 안전하게 관리
python-dotenv>=1.0.1
